<nav class="navbar navbar-default <%= 'navbar-fixed-top' if @fixed_nav %>">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Menu</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand">Samuria Jack Voting Game</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to 'Enemies', root_path, class: "#{'current' if @title == 'Vote Enemies'}" %></li>
            <li><%= link_to 'Allies', allies_path, class: "#{'current' if @title == 'Vote Allies'}" %></li>
            <li><%= link_to 'Outfits', outfits_path, class: "#{'current' if @title == 'Vote Outfits'}" %></li>
            <li><%= link_to 'Results', results_path, class: "#{'current' if @title.include?('Results')}" %></li>
          </ul>
        </div>
      </div>
    </nav>