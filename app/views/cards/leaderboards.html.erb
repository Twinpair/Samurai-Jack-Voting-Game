<% @cards.each do |card| %>
  <div id="card_<%= card.id %>">
    <hr>
    <p><%= card.id %></p>
    <p><%= card.name %></p>
    <p><%= card.description %></p>
    <p><%= image_tag card.picture.url, size: '300' %></p>
    <p><%= card.votes %></p>
    <hr>
  </div>
<% end%>